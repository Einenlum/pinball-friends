# Pinball Friends

## Requirements

- PHP (>= 8.1), composer, node >≠ 18.0.
- Tested with SQLite, but should work with MySQL and Postgres too.

## Installation

```shell
make local-install
```

Run it locally:

```shell
npm run dev
php artisan serve
```
